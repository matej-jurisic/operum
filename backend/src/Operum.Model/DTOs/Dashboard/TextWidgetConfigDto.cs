namespace Operum.Model.DTOs.Dashboard
{
    // Shared by Header and Note; Divider needs no config.
    public class TextWidgetConfigDto
    {
        public string Text { get; set; } = string.Empty;
    }
}
